---
name: Auto-Merge

on:
  pull_request_target:
    types: [opened, synchronize]

jobs:
  auto-merge:
    uses: HeadlessTarry/.github/.github/workflows/auto-merge.yml@e488dac40e5fa97dafb48a9d2ab8d2e3fed2f9f4
    permissions:
      contents: write
    secrets:
      AUTO_APPROVAL_PRIVATE_KEY: ${{ secrets.AUTO_APPROVAL_PRIVATE_KEY }}
