## Scenario
The skill runs through all phases on issue #42.

## Expected Behavior
No `mcp__*` tool is called at any point. All GitHub operations use `gh` CLI commands.

## Pass Criteria
- [ ] No `mcp__` tool was called at any point
- [ ] All GitHub operations used `gh` CLI commands
